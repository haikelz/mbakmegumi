apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mbakmegumi-tls
spec:
  secretName: mbakmegumi-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - megumi.ekel.dev
